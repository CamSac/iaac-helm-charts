---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cameran-lai
  namespace: default
spec:
  secretName: home-cert
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "home.cameran-lai.com"
  dnsNames:
    - "home.cameran-lai.com"
